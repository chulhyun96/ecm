spring:
  application:
    name: Hello

  datasource:
    url: jdbc:mariadb://localhost:3306/ecommerce?serverTimezone=UTC&characterEncoding=UTF-8
    username:
    password: root
    driverClassName: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false


  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  cloud:
    openfeign:
      okhttp:
        enabled: true


mailgun:
  api:
    key: a51f5a7dcbe13391bcd8a9c53900fe34-d010bdaf-6d855d26

server:
  port: 8081
